# ===================================================================
# Spring Boot configuration for the "dev" profile.
#
# This configuration overrides the application.yml file.

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================
server:
    port: 8081
    
logging:
    level:
        ROOT: DEBUG
        vn.tcx.framework: DEBUG
        vn.tcx.eoffice.danhmuc: DEBUG
        com.netflix: OFF
spring:
    profiles:
        active: prod2
        #include:
        #    - swagger
            # Uncomment to activate TLS for the dev profile
            #- tls
    jackson:
        serialization:
            indent-output: true
    liquibase:
        enabled: false
        change-log: classpath:/migrations/master.xml
    h2:
        console:
            enabled: true
    datasource:
        type: com.zaxxer.hikari.HikariDataSource
        url: jdbc:oracle:thin:@192.168.1.219:1539:xe
        username: common_test1
        password: abcd1234
        hikari:
            poolName: danhmuc-pool
            maximum-pool-size: 2
            connection-test-query: SELECT 1 FROM DUAL
            connection-timeout: 60000
            auto-commit: false
            
    jpa:
        hibernate:
            ddl-auto: none
            dialect: org.hibernate.dialect.H2Dialect
        show-sql: true
        properties:
            hibernate.id.new_generator_mappings: true
            hibernate.connection.provider_disables_autocommit: true
            hibernate.cache.use_second_level_cache: false
            hibernate.cache.use_query_cache: false
            hibernate.generate_statistics: false
            #hibernate.cache.region.factory_class: com.hazelcast.hibernate.HazelcastCacheRegionFactory
            #hibernate.cache.hazelcast.instance_name: common
            #hibernate.cache.use_minimal_puts: true
            #hibernate.cache.hazelcast.use_lite_member: true
    #data:
    #    jest:
    #        uri: http://localhost:9200
    elasticsearch:
      rest:
        uris:
        - https://es.eoffice.greenglobal.vn
        username: elastic
        password: changeme
        
elasticearch:
  index:
    name: 'danhmuc_1.1'
    alias: 'danhmuc'
    type: 'doc'
    shard: 3
    replica: 2
    from: 0
    size: 10
    timeout: 60 
       
tcx:
    cors:
        allowed-origins: "*"
        allowed-methods: "*"
        allowed-headers: "*"
        
security:
  byPassApiSecure: false
  oauth2:
    spGroup: Application/eoffice-test2
    client: # SP eoffice-test2
      clientId: fG9UCY6TrJT9rthbRJeQ_IqOf6Ia 
      clientSecret: 7wWLpLGsYe245htTbrbrYmADdvwa

eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin@192.168.1.115:8761/eureka/
