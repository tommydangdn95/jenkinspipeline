# ===================================================================
# Spring Boot configuration for the "dev" profile.
#
# This configuration overrides the application.yml file.

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================
spring:
    profiles:
        active: prod2
        #include:
        #    - swagger
            # Uncomment to activate TLS for the dev profile
            #- tls
    liquibase:
        enabled: false
        change-log: classpath:/migrations/master.xml
    datasource:
        type: com.zaxxer.hikari.HikariDataSource
        url: jdbc:oracle:thin:@192.168.1.219:1539:xe   # test2
        username: uaa_test2         # test2
        password: abcd1234          # test2
        hikari:
            poolName: Hikari
            auto-commit: false
    jpa:
        show-sql: false
        properties:
            hibernate.id.new_generator_mappings: true
            hibernate.connection.provider_disables_autocommit: true
            hibernate.cache.use_second_level_cache: false
            hibernate.cache.use_query_cache: false
            hibernate.generate_statistics: true
            #hibernate.cache.region.factory_class: com.hazelcast.hibernate.HazelcastCacheRegionFactory
            #hibernate.cache.hazelcast.instance_name: common
            #hibernate.cache.use_minimal_puts: true
            #hibernate.cache.hazelcast.use_lite_member: true
    elasticsearch:
      rest:
        uris:
        - https://es.eoffice.greenglobal.vn
        username: elastic
        password: changeme
    index:
      name: 'nguoidung_1.1'
      alias: 'nguoidung'
      type: 'doc'
      shard: 3
      replica: 2
      from: 0
      size: 10
      timeout: 60

security:
  # byPassApiSecure: false
  oauth2:
    spGroup: Application/eoffice-prod2
    client: # SP eoffice-test2
      clientId: fG9UCY6TrJT9rthbRJeQ_IqOf6Ia    # test2
      clientSecret: 7wWLpLGsYe245htTbrbrYmADdvwa # test2
      
tcx:
    errbit:
        host: http://192.168.1.124:8088/notifier_api/v2/notices
        id: 1
        key: 4416be0b9fc2472fa2cf877dffa07d02
        env: prod2

feign:
  client:
    location: 
      gateway: https://test2-api-qn.eoffice.greenglobal.vn